# CodeWeb Frontend

React + Vite frontend for the CodeWeb Academy marketing and enrollment site.

## Run locally

1. Open a terminal in `frontend`
2. Run `npm install`
3. Run `npm run dev`

## API

The site calls the backend at `http://localhost:3500` for enrollment and login.
